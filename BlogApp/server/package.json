{
  "name": "blog_list",
  "version": "1.0.0",
  "description": "backend",
  "license": "ISC",
  "author": "Utkarsh",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "test": "NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../bloglist-frontend && npm run build && cp -r dist ../Blog_list ",
    "start:test": "NODE_ENV=test node --watch index.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "lodash": "^4.18.1",
    "mongoose": "^9.6.3"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
